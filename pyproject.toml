[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build]
include = ["src/**/*.py"]

[tool.black]
line-length = 120
exclude = '''
/(
    \.git
  | \.venv
  | venv
)/
'''

[project]
name = "DL25"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  # "torch",        # Manually install to match CUDA version
  # "torchvision",  # Manually install to match CUDA version
    "black==25.9.0",
    "pre-commit==4.3.0",
    "hydra-core==1.3.2",
    "torchinfo==1.8.0",
]
authors = [
  {name = "Cyril Moser"},
  {name = "Gent Serifi"},
  {name = "Nicola Studer"},
  {name = "Aaron Zeller"},
]

description = "Transformers as Algorithmic Reasoners: A 2D Blackboard Approach"
readme = "README.md"